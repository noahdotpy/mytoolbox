modules:
  # APPS:
  # TODO: Add wofi-emoji
  #
  # PLUGINS:
  # TODO: Add overview plugin (probably hyprexpo)
  # TODO: Add dynamic cursors (https://github.com/VirtCode/hypr-dynamic-cursors)
  - type: rpm-ostree
    repos:
      - https://copr.fedorainfracloud.org/coprs/solopasha/hyprland/repo/fedora-%OS_VERSION%/solopasha-hyprland-fedora-%OS_VERSION%.repo
      - https://copr.fedorainfracloud.org/coprs/tofik/nwg-shell/repo/fedora-%OS_VERSION%/tofik-nwg-shell-fedora-%OS_VERSION%.repo
    install:
      # FIXME: hyprland-devel (for plugin) is not working due to dependency resolution issues
      # - hyprland-devel

      - SwayNotificationCenter
      - cliphist
      - hypridle
      - hyprland
      - hyprlock
      - hyprpaper
      - hyprpicker
      - hyprshot
      - hyprsunset
      - kde-connect-nautilus
      - nautilus
      - nwg-look
      - polkit-kde
      - pyprland
      - qt5-qtwayland
      - qt6-qtwayland
      - sushi
      - udiskie
      - waybar
      - wlogout
      - wofi
      - xdg-desktop-portal-hyprland
      - zenity

  - type: files
    files:
      - source: system/horizon
        destination: /

  - type: script
    scripts:
      - horizon/removeunprofessionalwallpapers.sh
      - horizon/sethyprlandwaybarmodules.sh
