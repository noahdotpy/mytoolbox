modules:
  # APPS:
  # TODO: Add wofi-emoji
  #
  # PLUGINS:
  # TODO: Add overview plugin (probably hyprexpo)
  # TODO: Add dynamic cursors (https://github.com/VirtCode/hypr-dynamic-cursors)
  - type: rpm-ostree
    install:
      # REMOVED:
      # cliphist (nix:cliphist)
      # hyprpolkitagent (fedora:polkit-kde)
      # hyprshot (nix:hyprshot)
      # hyprsunset (incompatible: only 0.45.0 and above)
      - SwayNotificationCenter
      - hypridle
      # - hyprland
      - hyprland-devel
      - hyprlock
      - hyprpaper
      - hyprpicker
      - kde-connect-nautilus
      - nautilus
      - nwg-drawer
      - polkit-kde
      - qt5-qtwayland
      - qt6-qtwayland
      - sushi
      - udiskie
      - waybar
      - wlogout
      - wofi
      - xdg-desktop-portal-hyprland

  - type: files
    files:
      - source: system/horizon
        destination: /

  - type: script
    scripts:
      - horizon/removeunprofessionalwallpapers.sh
      - horizon/sethyprlandwaybarmodules.sh
