name: Stable Channel (weekly)
on:
  schedule:
    - cron: "00 07 * * 2" # 07:00 UTC every Tuesday
  workflow_dispatch:
jobs:
  bluebuild:
    name: Build Custom Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        recipe:
          - images/aurora/dx/stable/aurora-dx--stable.yml
          - images/aurora/regular/stable/aurora--stable.yml
          - images/bluefin/dx/stable/bluefin-dx--stable.yml
          - images/bluefin/regular/stable/bluefin--stable.yml
          - images/hyra/dx/stable/hyra-dx--stable.yml
          - images/hyra/regular/stable/hyra--stable.yml

    steps:
      - name: Build Custom Image
        uses: blue-build/github-action@v1.7
        with:
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ${{ github.event.number }}
