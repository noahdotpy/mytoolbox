#!/usr/bin/bash

# I Don't Want To Network (IDWTN)

if [ -z "$1" ]; then
	echo "use arg 1 as: host (example: facebook.com)"
	exit 1
fi

echo "0.0.0.0 $1" >>/etc/hosts.d/idwtn-blocked.conf

systemctl start systemd-hosts.d.service
